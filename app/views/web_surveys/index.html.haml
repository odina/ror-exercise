%p#notice= notice
%h1 Listing Web Surveys
%table
  %thead
    %tr
      %th Title
      %th Shortlink slug
      %th{:colspan => "3"}
  %tbody
    - @web_surveys.each do |web_survey|
      %tr
        %td= web_survey.title
        %td= web_survey.shortlink_slug
        %td= link_to 'Show', web_survey
        %td= link_to 'Edit', edit_web_survey_path(web_survey)
        %td= link_to 'Destroy', web_survey, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Web survey', new_web_survey_path
